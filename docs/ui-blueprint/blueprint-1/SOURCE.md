# UI snapshot source

This folder contains the pinned Papership reference application (Vite / React `/cc-org-dash`) copied from commit `8a843bd6429faf1ace5a9eb6dcfb7440703d34c4` on 2026-09-11.

- Application sources: `src/`, `public/`, `package.json`, `package-lock.json`, and the original Vite/Tailwind configs.
- Written spec: `../ui-blueprint.md`.
- State captures: `*.png` and `capture.mjs` (not part of the original GitHub tree).
- Do not run `npm install` or a dev server from this folder (LIC-12 / LIC-13). The live Papership product UI is the copy in `apps/web`. This folder stays the pinned template.
