# 2026-09-12 continuation

- Owner directed a hybrid of live chrome and `docs/ui-blueprint/blueprint-2`, leaning toward the newer mock, plus a product rename to **Papership**.
- Web shell now uses the purple nav field, centered search, field-chrome icon buttons, Hey Engine rainbow pill, and notched tabs on the field. Home gained the compose strip. Auth card leans toward the mock while keeping Google sign-in.
- D-20 recorded. D-10 kept as historical (display name only superseded). Live slugs left in place: GitHub repo `OrgOS`, App `orgos-dev`, Vercel project `orgos`, `~/.config/orgos/`, `GITHUB_APP_REPO=OrgOS`, asset `papership-icon.png`, route `/cc-org-dash` (alias `/papership`).
- Local folder rename `~/OrgOS` → `~/Papership` is safe after reopen in Cursor. Do not rename GitHub/Vercel/App until those providers are updated.
- Owner reversed the hybrid: use blueprint-2 **exactly**. `/cc-org-dash` now mounts `apps/web/src/blueprint2/App.jsx` (tokens, auth without Google, notched field tabs, Company rail, Today compose/health/priorities/decisions/runs, docked 420px Hey Engine, status bar). D-21 recorded. Desktop Tauri and R4 mobile bottom tabs were not part of this port. Browser Today shell verified on http://127.0.0.1:5173/cc-org-dash; click-through MCP remains policy-blocked.
- Owner: prism-head mark is app icon and browser tab only. Removed in-app `<img>` uses from blueprint-2 (top bar, auth, compose, Hey), leftover cc-org-dash auth/home, and desktop Wordmark/SignInForm. Favicon / PWA / Tauri icons kept. D-21 item 6.
- Status review (no product code): we are in **Phase 4 implementation**, not planning. T4-0 and T4-1 are done (`SEAT_TEMPLATES`, `POST /members/invites`, SE T4-1 PASS). T4-2…T4-11 and Phase 4 PM/UI/UX/Security/Growth/PL artifacts are outstanding. No files in `/memory/blockers/`. Immediate pause is owner visual review of the blueprint-2 port. OQ-G2 legal basis blocks a live second seat, not T4-2 notice/gate code. Do not generate Phase 5 until G5. Roadmap, workstream manifest, and R1 checklist still say Phase 4 “not implemented” (documentation drift).
- Owner closeout 2026-09-12 (sections 1–9): visual review approved (D-22); OQ-G2 accepted (D-23); Gmail/Slack proceed dry-run (D-24); security pass accepted (D-25); in-repo slug papership/Papership (D-26); R1 residuals captured (worker consumer, AUTH-12 HTTP, usage first-baseline); T4-2…T4-11 finished; OQ-2/3/4/G1 closed (D-27…D-30); doc drift corrected. API pytest 66 passed. G5 PASS. Phase 5 not generated. Three execution phase plans remain (5, 6, 7). Owner queue: `docs/handover/phase-4-owner-actions.md`.
- Owner asked to log outstanding actions/decisions and proceed with next-phase planning (2026-09-12). D-31 recorded. Canonical log: `docs/handover/outstanding-actions-and-decisions.md`. Phase 5 plan drafted at `docs/plans/phase_5_company_operations_plan.md` (R3 / intake 10; G7/G8; status draft). No T5 implementation. Two execution phase plans remain after Phase 5 (6, 7). Provisional: B03 native capacity only; B01 KPIs `not_captured` without a source; adaptive views are tenant-stored JSON on trusted blueprint-2 components.
- Owner asked to implement Phase 5 (2026-09-12). T5-0…T5-12 done. API 77 passed; worker 33; static title scan pass. Memory ops, AUTH-29 views, personalisation off, native B01/B03, R3 shells, references, Automate schedules. G7 PASS. Phase 6 not generated. Browser click-through of `/cc-org-dash` not run.
- Owner asked to plan Phase 6 after clicking through leftover `/cc-org-dash` and authorized renaming it (2026-09-12). D-32: canonical mount `/papership`; `/` and old aliases redirect; storage keys `papership-auth`/`papership-theme` with one-time migrate from `cc-org-dash-*`. D-33: Phase 6 plan drafted at `docs/plans/phase_6_commercial_delivery_plan.md` (R4 / intake 11; G9/G10; status draft). No T6 implementation. One execution phase plan remains after Phase 6 (7).
- Browser click-through 2026-09-12 on http://127.0.0.1:5173: `/` and `/cc-org-dash` both end at `/papership`. Sign-in title Papership / Engine Labs. Tabs Today (Overview/Decisions/Running/Registry), Work (Issues/Board), Inbox honest-empty, People/Teams honest-empty without JWT, Data/Files/Integrations/Settings all opened. Avatar → Memory honest-empty. Hey Engine docked composer accepted typed text (not submitted). Company rail Decisions + a priority opened. Theme Light/Dark/Dimmed. No leftover `cc-org-dash` chrome. No currency prices; Data cost band says “no published prices”. **Fixture leak:** Today compose shows “Remaining allowance: 184640” — unpublished quantity, T6-3 must replace with a band. Settings → Plan is still appearance/theme, not D-29 tiers. Data still shows blueprint fixture counts (Traces 128) — leftover chrome, not a rename regression. Screenshots under `/tmp/computer-use/`.
- Owner authorized the leftover OrgOS/cc-org sweep to Papership in the current tree (2026-09-12). Pass A done: NOTICE, grants alias, `papership-token`, store path, Vercel runbook rename. Provider-locked files listed in `docs/handover/rename-owner-first-providers.md` wait for the owner to rename GitHub / App / Vercel, then a follow-up updates those files. Phase 6 not implemented.
- Owner asked to remove named-competitor comparison tables and for a resume handover before renaming GitHub (session context will drop). Tables and leftover table-describing prose withdrawn from blueprint/roadmap/Growth/PL. Attach icon export renamed. Exhaustive resume pack finalized: `docs/handover/resume-after-provider-rename.md`. Still do not implement Phase 6 until asked. Still do not flip provider-locked files until the owner finishes GitHub / App / Vercel. Owner may now rename those providers and open a new chat with this file plus the saved transcript.
- Owner will ignore the session transcript and rely on the handover only. §0 of `docs/handover/resume-after-provider-rename.md` is now the full owner naming-change checklist (GitHub, App, Vercel, host `GITHUB_APP_REPO`, optional config dir, git remote, local folder, verification boxes, do-not-touch). Next chat: paste that file only.
- New App `papership-dev` verified: App ID `4918983`, installation `161090499`, selected repos. PEM filename `papership-dev.2026-09-12.private-key.pem` under `~/.config/papership/`. Agent hooks block chmod and writes under that dir. Current API auth is App ID + installation ID + PEM only; GitHub App OAuth client id/secret are unused. `orgos-dev` no longer appears on the org installation list.
- VPS check 2026-09-12: SSH `hermes-vps` (hermes) and `droplet-campbell` (root) both land on `hermes-droplet-campbell`. No Papership/OrgOS tree, no `~/.config/orgos` or `~/.config/papership`, no `GITHUB_APP_*` files, no `:8000` listener. Hermes units have no GitHub App env. Did not copy the PEM onto Hermes (T-33 / AUTH-22: App key is API-only). `open_pull` host is the local Mac API, already updated. `ml-compute` SSH host is a placeholder and did not resolve. Vercel slug still residual.
- Locked-file follow-up done: `.env.example` and `scripts/dev-local.sh` default `GITHUB_APP_REPO=papership` and prefer `~/.config/papership/github-app.paths`; `github_app.py` writes `.papership/loop/` with commit prefix `papership:` and dual-reads `.orgos/loop/`. Assumption: GitHub canonical repo name is lowercase `papership`. API venv recreated after folder rename (old interpreter path `~/OrgOS/...` was broken). Validation: `services/api/.venv/bin/python -m pytest` GitHub/loop/env 19 passed; `apps/web/tests/static-scan.test.mjs` 4 passed; live `GET http://127.0.0.1:8000/health` → `"github":"reachable"`. Hermes still `unreachable` locally (expected without tunnel). Vercel project slug remains an owner residual. Nothing committed.
- Phase 6 first slice (2026-09-12, owner implement ask): `allowances` + `allowance_reservations` (band enums, D-29 labels); reserve/reconcile; exhausted pauses new work; `/billing/charge` 403; Today allowance fixture → `not captured`; Settings → Plan shows Free/Basic/Professional/Enterprise with no prices. `ENGINE_BILLING_CHARGES_ENABLED` default false. Stripe test names in `.env.example` only (not allowlisted). Tests: `test_phase6.py` + authz/env 16 passed; static scan 5. T6-5…T6-8 and six role handoffs remain. Cloud Agent launch is not available from this chat’s tools; work continues on the local tree and is pushed.
- Rename closeout 2026-09-12: copied `hermes-api-server.env` and `github-app.env` into `~/.config/papership/` (mode 600; old `~/.config/orgos/` kept as fallback, old PEM not copied). Runbook renamed to `papership-hermes-tunnel.md`. Vercel project `prj_S74JOIky7KugVTrfu652NhJYOL8l` is already named `papership`; Git link still lists `enginelabs-au/OrgOS` (redirects). Owner authorized Phase 6 implementation. Stash@{0} files recovered into the tree (tunnel, github-app handover, runbook); Tauri `src-tauri/gen/schemas` stay gitignored. stash@{1} left unapplied (stale main STATE).
- Owner icon supersession 2026-09-12: new prism-head JPEG cut to a transparent `brand/papership-icon.png` master (purple field and outer black removed; cream/blue mark restored). Regenerated web favicon/PWA (`favicon.ico`, 16/32, 192/512, maskable 512, apple-touch 180 opaque `#16082e`), desktop public + Tauri png/icns/ico, and blueprint `assets/papership-icon.png` (opaque plate so cream chips read). Deleted `orgos-icon.png` slots and the leftover blueprint upload of the prior mark. All live `orgos-icon` path refs now `papership-icon`. No native Android/iOS app trees; PWA + apple-touch cover those. GitHub org/App dashboard avatars remain owner-only.
- Icon correction 2026-09-12: owner clarified the purple rounded plate is the icon. Regenerated from `icon2-802a18f2-…jpg`. Only outside black corners and connected dark-red fringe are transparent (~80k px). Purple field kept. iOS apple-touch / Android PWA / ICO / ICNS flatten leftover corner alpha to black. Hey Engine unpublished `158` replaced with `not captured`. T6-5 `/licenses` + rate-card schema. G9 PASS with residuals (CA-10, D-25). pytest 8; static scan 5. Phase 7 not generated.
- Vercel recheck 2026-09-12: project `papership` Git link is already `enginelabs-au/papership` (no relink needed). Stale OrgOS note was from an earlier MCP read. Latest production deploy failed on `npm run build -w @orgos/web` while `apps/web` is `@papership/web`; patched `vercel.json` on `main` (`20b7fc8`).
- Icon fringe trial on `apps/desktop/public/papership-icon.png` only: 1151 dark-red rim pixels recolored to nearby plate purple. Boat reds left alone (bright, interior). Do not strip all red globally.
- Pushed 2026-09-12 as Cursor `<cursoragent@cursor.com>`: `4acf450` on `cursor/phase-6-planning-cc89` (`Ship phone chrome and cut the icon's red rim.`). Working tree was clean at push. `main` was not updated (still `20b7fc8` Vercel workspace rename). Phase 7 planning is unblocked; implementation waits for the plan file plus an owner ask.
- Icon rim 2026-09-12: owner said leftover red around the purple squircle was still obvious. Tried erode, selective recolor, and inset squircle. Winner: 8px-inset iOS rounded-square mask (cuts the maroon rim before it) plus recolor of any remaining non-purple edge pixel to nearby plate purple. Boat reds untouched (interior). Edge-red count 434 → 0. Regenerated brand, web, desktop, Tauri png/ico/icns, PWA, apple-touch, blueprint asset via `scripts/generate-papership-icons.py`.
- Owner asked 2026-09-12 to adapt all mobile screens from desktop to the attached R4 phone layouts. Live `/papership` now uses `OrgOS Mobile.dc.html` rules at `max-width: 767px`: compact header (rail, wordmark, search, bell, avatar), bottom tabs Today/Work/Inbox/Hey Engine, other tabs in the left rail sheet, Hey Engine as a full-screen sheet, 2×2 health, one Overview decision with 44px Approve/Reject, compact running rows, stacked Inbox/Files/Settings/Work board scroll. Auth offers Face ID/fingerprint above the password (browser still uses email/password). Allowance line stays `not captured`. D-21 item 5 updated. Today verified at 390×844; desktop 1280×800 still docked. Static scan 6. MCP clicks for Work/Inbox/Hey were hook-blocked. This is web compressed chrome, not Phase 7 native clients.
- Owner asked 2026-09-12 to proceed with the Phase 7 plan. D-34 recorded. Plan drafted at `docs/plans/phase_7_ecosystem_mobile_plan.md` (R4 / intake 12; G11/G12; status draft). Scope: P19 pack contract + plugin trust; deny-by-default B14/B19/B20/B21/B24 shells; B14 proposals + reauth (no payout); PWA `start_url` `/papership` + explicit offline; `apps/mobile` Tauri-2 wrap of `apps/web` (Capacitor fallback); B20 offline evidence queue. Non-goals: redo web chrome, Swift/Kotlin rewrite, store submit, CA-10, live source connectors, D-25 lift. After G11 the next file is `docs/plans/final_implementation_checklist.md`. T7-1…T7-9 not started.
- Owner asked 2026-09-12 to implement Phase 7. T7-1…T7-9 done. G11 PASS with residuals. pytest 30 (phase5/6/7/env); static scan 7. PWA `start_url` `/papership` + `sw.js`. Packs/trust/custom-fields; B14 reauth no payout; B20 idempotent evidence; R4 shells deny live write. `apps/mobile` Tauri wrap + toolchain report (rustc/Xcode/adb present; no simulator screenshots; no store binary). Registry B14/B19/B20/B21/B24/P19 `configured`. Final checklist not generated.
- Owner asked 2026-09-12 to close initial development: read every phase plan + blueprint, write the final checklist, implement leftovers that do not need owner input. Done: Data/health honesty (no Traces 128 / fake p95); Settings → Data OQ-G2 + erasure-intent buttons; `POST /erasure/request` records only (reauth, three words, `destroyed` false); leftover `$0.` stripped from unused DataScreen; `scripts/era15-dry-run.mjs` list-only; `docs/plans/final_implementation_checklist.md` rewritten as the R1–R4 owner walk. Super-critical leftovers: CA-10 (two decisions), D-25 lift, production publish, paid stores, live OAuth apps, real destroy.
- Owner asked 2026-09-12 to move project blueprints into `docs/blueprints/`. Intake is now `docs/blueprints/company_agent_system_blueprint.md`. UI spec is `docs/blueprints/ui-blueprint.md`. Strategy assessment was already there (`2026-09-10_engine_labs.md`). Capture/snapshot folder `docs/ui-blueprint/` (blueprint-2 HTML, PNGs) stayed put so D-21 paths keep working. References updated across plans, policies, decisions, workstream artifacts, and READMEs.
- Owner answered closeout §5 (2026-09-12): research prices (do not publish); keep charges off; request D-25 lift; merge to main; stores later; Gmail/Slack walk; explain infra. Security: [Security Review](456e859e-3ffe-48e8-8fe3-c8a0a2ce0995) + [D-25 Hermes write gate](47859d89-6e71-416d-ae15-e0c05433bc26) — lift **not authorized**. Charges not enabled.
- Merge 2026-09-12: feature branches 4/5/6 are ancestors of `cursor/phase-6-planning-cc89`. Did not merge `origin/orgos-loop-r1-close` (stale 5-line OrgOS loop note). Closeout commit `9237e98`, merge `7b1207e` on local `main`. Conflicts resolved: latest Papership icons + brand note with 8px iOS squircle. `git push origin main` rejected GH007 (`shuffle.ops@gmail.com`). Production already serves `/papership` from `20b7fc8` (`orgos-ivory.vercel.app`); Phase 7 closeout is not on origin yet.
- Owner 2026-09-13: trial competitor-average pricing; delete leftover branches; git author cursoragent / login cam-douglas; reuse Hermes env for Gmail/Slack; explain destroy-infra; OQ-G2 failed to fetch; update desktop chrome. D-35 published Free/Pro/Max/Enterprise (50k/200k/800k/400k, $0/$24/$120/$32, usage 1–5). Charges still off. Desktop Tauri now loads `@papership/web` `/papership`. Hermes units inactive; mailbox/Slack Hermes env is not Papership OAuth — not copied. OQ-G2 needs local API; Vercel cannot store the flag. Extra local/remote feature branches deleted after author rewrite + push.
