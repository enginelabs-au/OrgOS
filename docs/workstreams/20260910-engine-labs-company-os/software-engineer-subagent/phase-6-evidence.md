# Software Engineer evidence — Phase 6

- `services/api/tests/test_phase6.py`: allowance ≠ grant; unpriv create 403; unknown band 400; reserve/reconcile/duplicate 409; exhausted pauses new work and records stay 200; `/billing/charge` 403; rate-card unpublished; `/licenses` identifiers only.
- Settings → Plan: D-29 labels. Today compose and Hey Engine: `not captured` (no `184640` / `158`).
- Stripe names in `.env.example` only. `ENGINE_BILLING_CHARGES_ENABLED` default false.
